[flake8]
max-line-length = 100

[coverage:report]
exclude_lines =
    pragma: no cover
    raise NotImplementedError

[tool:pytest]
markers =
    timing: timing tests that are affected by coverage (deselect with '-m "not slow"')
